# AUMARA A2A delivery evidence log (JSONL). Appended by sandbox/runtime; empty until first receipt.

## Agent promo (optional)
Set Vercel **Preview** env `AUMARA_AGENT_PROMO_JSON` to the JSON in `agent-facing-promo-stub-20260918.json` (or `{...promo fields...}`). Guest site copy is untouched; promo is agent-facing only. Omit or invalid JSON → `promo: null`.
