/* Cesium Ion configuration
 * Ion credentials are never stored in browser storage, source, URLs, or logs.
 * The local AUMARA twin works without Ion.
 */
window.AUMARA_ION = {
  asset: 2275207,
  apply: function () { return false; },
};
